const getIconSrc = (option, platform) => {

    if(option === 'frontend'){
        const iconSrc = '/public/icons/'+option+'/' + platform + '.svg'
        return iconSrc

    }
        return icons
    }

    

}

export default getIcons